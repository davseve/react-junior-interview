import ProductItem from './ProductItem';
const ProductList = () => {
    return (
        <div>
            <h1>products</h1>
            {
                // iterate over the products array and display the details of each product
            }
        </div>
    );

}

export default ProductList;